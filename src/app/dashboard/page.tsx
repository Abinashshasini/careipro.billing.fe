'use client';

export default function DashboardPage() {
  return (
    <div className="p-6">
      {/* <h1 className="text-xl font-bold">Welcome {store?.name}</h1>
      <p className="text-gray-600">Market: {store?.market}</p>
      <p className="text-gray-600">City: {store?.city}</p> */}
    </div>
  );
}
